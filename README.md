# Architecture Teaching Assistant MVP

## Setup

1. Install dependencies:
   - `npm install`
2. Create environment file:
   - `cp .env.example .env`
3. Put your API key in `.env`:
   - `OPENAI_API_KEY=...`
4. Run the app:
   - `npm run dev`

Open [http://localhost:3000](http://localhost:3000).
